<IfModule mod_rewrite.c>
  RewriteEngine On

  # Serve existing files (CSS/JS/images)
  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^ - [L]

  # SPA fallback
  RewriteRule . /index.html [L]
</IfModule>
